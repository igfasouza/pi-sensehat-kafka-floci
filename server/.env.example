# If producer runs on this same Pi, localhost is fine.
# For producers on another machine, use the Pi LAN IP, e.g. 192.168.1.50
PI_HOST=localhost

# Recommended on Pi: point this to your USB SSD, e.g. /mnt/ssd/kafka-lab
DATA_ROOT=./data
